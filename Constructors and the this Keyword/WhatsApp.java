class WhatsApp {
    String version;
    int users;
    boolean encrypted;

    WhatsApp(String version, int users, boolean encrypted) {
        this.version = version;
        this.users = users;
        this.encrypted = encrypted;
	}
}